#include <windows.h>

#include "InputConstants.h"

namespace KeyConstants {
	const char LANE_KEYS[GAME_LANE_COUNT] = { 'A', 'Z', 'E', 'R' };
	const char MENU_PREVIOUS = VK_UP;
	const char MENU_NEXT = VK_DOWN;
	const char MENU_CONFIRM = VK_RETURN;
}